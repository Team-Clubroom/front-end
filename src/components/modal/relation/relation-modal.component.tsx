import { Modal } from "../modal.component.tsx";

export const RelationModalComponent = () => {
  return (
    <Modal title={"Relation"} isOpen={true} close={() => {}}>
      hello
    </Modal>
  );
};
